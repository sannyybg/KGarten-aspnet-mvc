@model Kindergarten3.Models.ActivateTable

@{
    ViewBag.Title = "ActivationTable";
}

<h2>აკადემიური რეგისტრაციის აქტივაცია</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ActivateTable</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ActivateTableId)

    <div class="form-group">
        @*@Html.LabelFor(model => model.ActivateStatus, htmlAttributes: new { @class = "control-label col-md-2" })*@
        <label class = "control-label col-md-2">საგნების არჩევის აქტივაცია</label>
        <div class="col-md-10">
            <div class="checkbox">
                @Html.EditorFor(model => model.ActivateStatus)
                @Html.ValidationMessageFor(model => model.ActivateStatus, "", new { @class = "text-danger" })
            </div>
        </div>
    </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("მთავარი გვერდი", "Index", "Admin")
</div>
